#include <stdio.h>

/**
 * main - Entry of the programm
 * @argc: argument counter
 * @argv: arguments array
 * Return: return 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	(void)argv;
	return (0);
}
